package com.techelevator.farm;

/*interface is contract. interfaces are implemented. any class that implements interface
must provide method bodies for the abstract methods listed in the interface.*/

public interface Singable {

    //abstract method < no body
    //implicitly public
    String getSound();
    String getName();
}
